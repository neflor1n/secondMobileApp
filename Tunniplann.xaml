<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                x:Class="secondMobileApp.Tunniplann"
             Title="Schedule">

    <ScrollView>
        <Grid x:Name="scheduleGrid">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Заголовки таблицы -->
            <Label Text="Subject" HorizontalTextAlignment="Center" Grid.Row="0" Grid.Column="0"/>
            <Label Text="Teacher" HorizontalTextAlignment="Center" Grid.Row="0" Grid.Column="1"/>
            <Label Text="Room" HorizontalTextAlignment="Center" Grid.Row="0" Grid.Column="2"/>
        </Grid>
    </ScrollView>
</ContentPage>
